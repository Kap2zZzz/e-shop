@model IEnumerable<string>

@*@Html.ActionLink("Home", "List", "Product", null, new { @class = "btn btn-block btn-default btn-lg" })*@
@Html.ActionLink("Усі товари", "List", "Product", null, new { @class = "list-group-item" + ("Усі товари" == ViewBag.SelectedCategory ? " active" : "") })

@foreach (var link in Model)
{
    @*@Html.RouteLink(link, new { controller = "Product", action = "List", category = link, page = 1 }, new { @class = "list-group-item" + (link == ViewBag.SelectedCategory ? " active" : "") })*@
    @Html.ActionLink(link, "List", "Product", new { category = link, page = 1 }, new { @class = "list-group-item" + (link == ViewBag.SelectedCategory ? " active" : "") })

}